# Recorded LLM API responses for optional replay in tests.
#
# Add JSON fixtures here when stabilizing integration tests without live API calls.
# Live integration tests are in tests/test_ai_integration.py (`pytest -m integration`).
